hxbehave is a resumable behavior tree library for Haxe. Supports common behavior 
tree node types and allows yielding intermediate values and resuming iteration 
later.

To run tests: `haxe test.hxml && neko test.n`
